apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keycloak
  namespace: argocd
spec:
  destination:
    name: ''
    namespace: shared
    server: 'https://kubernetes.default.svc'
  source:
    path: env/devops/shared/keycloak
    repoURL: 'https://github.com/logamanig/sample-argocd-apps'
    targetRevision: master
  project: tools
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
